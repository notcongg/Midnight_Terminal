[project]
name = "midnight-terminal"
version = "0.1.0"
description = "A terminal written in Python"
requires-python = ">=3.12"

dependencies = [
    "psutil",
    "wmi",
    "colorama",
    "prompt-toolkit",
    "python-dotenv",
    "openai",
]
